# YOLO11 with P2 detection head only (no attention, for ablation study)
# P2/4, P3/8, P4/16, P5/32 detection heads

nc: 10
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]           # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]          # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]   # 2 (P2/4 backbone feature)
  - [-1, 1, Conv, [256, 3, 2]]          # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]   # 4 (P3/8 backbone feature)
  - [-1, 1, Conv, [512, 3, 2]]          # 5-P4/16
  - [-1, 2, C3k2, [512, True]]          # 6 (P4/16 backbone feature)
  - [-1, 1, Conv, [1024, 3, 2]]         # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]         # 8
  - [-1, 1, SPPF, [1024, 5]]            # 9
  - [-1, 2, C2PSA, [1024]]              # 10 (P5/32 backbone feature)

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11: up P5
  - [[-1, 6], 1, Concat, [1]]                     # 12: cat + P4 backbone
  - [-1, 2, C3k2, [512, False]]                   # 13: P4-up

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 14: up P4
  - [[-1, 4], 1, Concat, [1]]                     # 15: cat + P3 backbone
  - [-1, 2, C3k2, [256, False]]                   # 16: P3-up

  # P2 scale
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 17: up P3
  - [[-1, 2], 1, Concat, [1]]                     # 18: cat + P2 backbone
  - [-1, 2, C3k2, [128, False]]                   # 19: P2 neck -> Detect P2

  - [-1, 1, Conv, [128, 3, 2]]                   # 20: P2->P3 down
  - [[-1, 16], 1, Concat, [1]]                     # 21: cat + P3-up
  - [-1, 2, C3k2, [256, False]]                   # 22: P3-down -> Detect P3

  - [-1, 1, Conv, [256, 3, 2]]                   # 23: P3->P4 down
  - [[-1, 13], 1, Concat, [1]]                     # 24: cat + P4-up
  - [-1, 2, C3k2, [512, False]]                   # 25: P4-down -> Detect P4

  - [-1, 1, Conv, [512, 3, 2]]                   # 26: P4->P5 down
  - [[-1, 10], 1, Concat, [1]]                     # 27: cat + P5 backbone
  - [-1, 2, C3k2, [1024, True]]                   # 28: P5-down -> Detect P5

  - [[19, 22, 25, 28], 1, Detect, [nc]]
